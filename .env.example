# Google Gemini API Key (for natural language CA parsing)
GEMINI_API_KEY=

# Supabase Credentials (optional for cloud sync, app defaults to local persistence if blank)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
